## Installation

Before installing and running this project, ensure that [DFTB+](https://www.dftbplus.org/) is installed on your system, as it is required for all quantum mechanical simulations. After installation, find the `dftb+` executable. This path will be required by the package to run simulations.

Install PyTorch and PyTorch Geometric dependencies first:
```bash
pip install torch>=2.8.0
pip install torch_geometric torch_cluster torch_scatter torch_sparse torch_spline_conv -f https://data.pyg.org/whl/torch-<torch_version>+<cuda_version>.html
```

Then install polygraphpy:
```bash
pip install polygraphpy
```

## Data
The `data/` directory contains all datasets, intermediate files, and outputs generated by the models.  
This structured organization ensures reproducibility and clarity throughout the project.  
The directories and files listed below are managed by Git unless otherwise noted.

- **full_dataset.csv**: The complete, unfiltered dataset. It includes columns for `id`, `smiles`, `cmpdname`, `mw`, `mf`, `polararea`, and `complexity`.
- **original_dataset.csv**: The original raw dataset containing extensive molecular information, such as `id`, `cmpdname`, `isosmiles`, `canonicalsmiles`, and various stereochemistry and physical properties.
- **reduced_dataset.csv**: A smaller subset of the data, likely used for testing or specific model runs.
- **polarizability_data.csv**: Processed data for monomer training, containing molecular features and polarizability values.
- **polarizability_data_copoly.csv**: Processed data for copolymer training.
- **ga_output/**: The output directory for the genetic algorithm, storing the final population and any related analysis.
- **generative_data/**: Stores datasets specifically prepared for the GPT-based generative model.
- **generative_output/**: The final output of the generative process, including the generated molecules and their properties.
- **gnn_output/**: Stores the trained GNN model for monomers, along with training statistics and performance metrics.
- **gnn_output_copoly/**: Stores the trained GNN model for copolymers and its corresponding outputs.

---

## Git-Ignored Directories

The following directories are intentionally ignored by Git as they contain large, generated, or temporary files:

- **molecules/**: Holds the output data from DFTB+ simulations, which is used to derive molecular properties.
- **training_input_data/**: Stores pre-processed monomer graphs in PyTorch Geometric format (`.pt` files) for the GNN model.
- **training_input_data_copoly/**: Stores pre-processed copolymer graphs for the GNN.
- **validation_data/**: The set of monomer graphs held out for model validation and prediction.
- **validation_data_copoly/**: The validation set of copolymer graphs.
- **xyz_files/**: Contains XYZ files for the molecules, typically used for molecular geometry visualization.
- **generative_model/**: Contains the trained GPT generative model.